# Финальное Задание: Менеджер Геометрических Фигур

## 📋 Описание проекта

Вам предстоит создать консольное приложение на Python для управления геометрическими фигурами. Программа должна поддерживать работу с различными типами фигур (круг, прямоугольник, треугольник), вычислять их площадь и периметр, а также предоставлять интерактивный пользовательский интерфейс.

---

## 🎯 Цели задания

1. **Применить принципы ООП**: наследование, инкапсуляция, полиморфизм
2. **Работать с исключениями**: обработка ошибок ввода и валидация данных
3. **Использовать модули Python**: `math` для математических вычислений
4. **Организовать код в пакеты**: создание модульной структуры проекта
5. **Работать с импортами**: понимание работы пакетов и модулей Python
6. **Создать консольное меню**: интерактивное взаимодействие с пользователем
7. **Применить защищенные атрибуты**: использование префикса `_` для атрибутов классов

---

## 📁 Структура проекта

Создайте следующую структуру файлов и папок:

```
final_task/
├── main.py                    # Главный файл с меню и точкой входа
├── shapes/                    # Пакет с классами фигур
│   ├── __init__.py           # Инициализация пакета shapes
│   ├── shape.py              # Базовый класс Shape
│   ├── circle.py             # Класс Circle
│   ├── rectangle.py          # Класс Rectangle
│   └── triangle.py           # Класс Triangle
└── utils/                     # Пакет с утилитами
    ├── __init__.py           # Инициализация пакета utils
    └── helpers.py            # Вспомогательные функции
```

### Что такое `__init__.py`?

Файл `__init__.py` превращает обычную папку в Python-пакет. Это позволяет импортировать модули из этой папки.

**Пример содержимого `shapes/__init__.py`:**
```python
from .shape import Shape
from .circle import Circle
from .rectangle import Rectangle
from .triangle import Triangle

__all__ = ['Shape', 'Circle', 'Rectangle', 'Triangle']
```

Это позволяет использовать простой импорт в `main.py`:
```python
from shapes import Circle, Rectangle, Triangle
```

---

## 🏗️ Этап 1: Создание структуры проекта

### Шаг 1.1: Создайте папки

1. Создайте главную папку проекта `final_task/`
2. Внутри создайте две подпапки:
   - `shapes/` - для классов фигур
   - `utils/` - для вспомогательных функций

### Шаг 1.2: Создайте файлы `__init__.py`

В каждой подпапке создайте файл `__init__.py` (о содержимом позже).

---

## ⚠️ ВАЖНО: Разница между импортами с точкой (.) и без точки

**ПРОЧИТАЙТЕ ЭТО ПЕРЕД ТЕМ, КАК НАЧНЕТЕ СОЗДАВАТЬ КЛАССЫ!**

### Правило простое:

| Где находится файл | Какой импорт использовать | Пример |
|-------------------|--------------------------|---------|
| **Внутри пакета** (в папке shapes/ или utils/) | **С ТОЧКОЙ (.)** - относительный импорт | `from .shape import Shape` |
| **Вне пакета** (файл main.py в корне) | **БЕЗ ТОЧКИ** - абсолютный импорт | `from shapes import Circle` |

### Почему так?

**Точка (.) означает "в текущем пакете":**
- Когда вы пишете `from .shape import Shape` в файле `shapes/circle.py`
- Точка говорит: "возьми модуль `shape` из ЭТОГО ЖЕ пакета (shapes/)"
- Это работает потому что `circle.py` находится ВНУТРИ папки `shapes/`

**Без точки - это абсолютный путь:**
- Когда вы пишете `from shapes import Circle` в файле `main.py`
- Нет точки - значит ищем пакет `shapes` от корня проекта
- Это работает потому что `main.py` находится СНАРУЖИ пакета

### Визуальная схема:

```
final_task/
├── main.py          ← ВНЕ пакета → БЕЗ ТОЧКИ: from shapes import Circle
│
└── shapes/          ← Это ПАКЕТ
    ├── __init__.py  ← ВНУТРИ → С ТОЧКОЙ: from .circle import Circle
    ├── shape.py     ← ВНУТРИ
    ├── circle.py    ← ВНУТРИ → С ТОЧКОЙ: from .shape import Shape
    ├── rectangle.py ← ВНУТРИ → С ТОЧКОЙ: from .shape import Shape
    └── triangle.py  ← ВНУТРИ → С ТОЧКОЙ: from .shape import Shape
```

### Примеры:

**✅ ПРАВИЛЬНО - файл shapes/circle.py (внутри пакета):**
```python
from .shape import Shape  # С ТОЧКОЙ!

class Circle(Shape):
    pass
```

**❌ НЕПРАВИЛЬНО - файл shapes/circle.py:**
```python
from shape import Shape  # БЕЗ ТОЧКИ - НЕ РАБОТАЕТ!
# Python не найдет модуль, т.к. ищет его в корне проекта
```

**✅ ПРАВИЛЬНО - файл main.py (вне пакета):**
```python
from shapes import Circle  # БЕЗ ТОЧКИ!

circle = Circle("red", 5)
```

**❌ НЕПРАВИЛЬНО - файл main.py:**
```python
from .shapes import Circle  # С ТОЧКОЙ - НЕ РАБОТАЕТ!
# main.py не внутри пакета, относительный импорт невозможен
```

### Запомните:

🔹 **Файл внутри shapes/?** → Импорт **С ТОЧКОЙ**: `from .shape import Shape`

🔹 **Файл main.py?** → Импорт **БЕЗ ТОЧКИ**: `from shapes import Circle`

🔹 **Файл __init__.py внутри пакета?** → **С ТОЧКОЙ**: `from .circle import Circle`

---

## 🔧 Этап 2: Базовый класс Shape

**Файл:** `shapes/shape.py`

### Задачи:

1. **Импортируйте модуль `math`** (понадобится для наследников)

2. **Создайте класс `Shape`:**
   - Конструктор `__init__(self, color)`:
     - Принимает один параметр: `color` (строка)
     - Сохраняет его в защищенный атрибут `_color`
   
   - Метод `get_area(self)`:
     - Не принимает параметров
     - Возвращает `0.0`
     - Это метод-заглушка для переопределения в дочерних классах
   
   - Метод `get_perimeter(self)`:
     - Не принимает параметров
     - Возвращает `0.0`
     - Также заглушка
   
   - Метод `__str__(self)`:
     - Возвращает строку с информацией о фигуре
     - Формат: `"Shape (Color: {цвет})"`

### Пример вывода:

```
Shape (Color: red)
```

---

## 🔵 Этап 3: Класс Circle (Круг)

**Файл:** `shapes/circle.py`

### Задачи:

**Импорты (смотрите объяснение выше!):**
```python
from .shape import Shape  # ← С ТОЧКОЙ! Файл внутри пакета shapes/
```

**Создайте класс `Circle`, который наследуется от `Shape`:**

   - **Конструктор `__init__(self, color, radius)`:**
     - Вызовите конструктор родителя с помощью `super().__init__(color)`
     - Проверьте, что `radius > 0`, иначе выбросьте `ValueError("Radius must be positive")`
     - Сохраните радиус в защищенный атрибут `_radius`
   
   - **Метод `get_area(self)`:**
     - Формула: S = π × r²
     - Используйте `math.pi` для значения π
     - Возведите радиус в квадрат: `self._radius ** 2`
   
   - **Метод `get_perimeter(self)`:**
     - Формула: P = 2 × π × r
     - Вычислите длину окружности
   
   - **Метод `__str__(self)`:**
     - Формат: `"Circle ({цвет}) | R: {радиус}"`

### Примеры вывода:

**Успешное создание:**
```python
circle = Circle("blue", 5.0)
print(circle)
# Вывод: Circle (blue) | R: 5.0

print(f"Area: {circle.get_area():.2f}")
# Вывод: Area: 78.54

print(f"Perimeter: {circle.get_perimeter():.2f}")
# Вывод: Perimeter: 31.42
```

**Обработка ошибки:**
```python
try:
    circle = Circle("red", -3)
except ValueError as e:
    print(e)
# Вывод: Radius must be positive
```

---

## 🟩 Этап 4: Класс Rectangle (Прямоугольник)

**Файл:** `shapes/rectangle.py`

### Задачи:

**Импорт (смотрите объяснение выше!):**
```python
from .shape import Shape  # ← С ТОЧКОЙ! Файл внутри пакета shapes/
```

**Создайте класс `Rectangle`, наследующийся от `Shape`:**

   - **Конструктор `__init__(self, color, width, height)`:**
     - Вызовите конструктор родителя
     - Проверьте, что `width > 0` И `height > 0`
     - Если нет - выбросьте `ValueError("Dimensions must be positive")`
     - Сохраните ширину в `_width`, высоту в `_height`
   
   - **Метод `get_area(self)`:**
     - Формула: S = width × height
   
   - **Метод `get_perimeter(self)`:**
     - Формула: P = 2 × (width + height)
   
   - **Метод `__str__(self)`:**
     - Формат: `"Rectangle ({цвет}) | {ширина} x {высота}"`

### Примеры вывода:

```python
rect = Rectangle("green", 10.0, 5.0)
print(rect)
# Вывод: Rectangle (green) | 10.0 x 5.0

print(f"Area: {rect.get_area():.2f}")
# Вывод: Area: 50.00

print(f"Perimeter: {rect.get_perimeter():.2f}")
# Вывод: Perimeter: 30.00
```

---

## 🔺 Этап 5: Класс Triangle (Треугольник)

**Файл:** `shapes/triangle.py`

### Задачи:

**Импорты (смотрите объяснение выше!):**
```python
import math
from .shape import Shape  # ← С ТОЧКОЙ! Файл внутри пакета shapes/
```

**Создайте класс `Triangle`, наследующийся от `Shape`:**

   - **Конструктор `__init__(self, color, a, b, c)`:**
     - Вызовите конструктор родителя
     - **Проверка 1:** Все стороны должны быть положительными
       - Если `a <= 0` ИЛИ `b <= 0` ИЛИ `c <= 0`
       - Выбросьте `ValueError("Sides must be positive")`
     - **Проверка 2:** Неравенство треугольника
       - Сумма любых двух сторон должна быть СТРОГО больше третьей
       - Проверьте три условия: `(a + b <= c)` ИЛИ `(a + c <= b)` ИЛИ `(b + c <= a)`
       - Если хотя бы одно истинно - `ValueError("Invalid triangle! Sum of two sides must be > third.")`
     - Сохраните стороны в `_a`, `_b`, `_c`
   
   - **Метод `get_perimeter(self)`:**
     - Формула: P = a + b + c
   
   - **Метод `get_area(self)` - Формула Герона:**
     - Шаг 1: Вычислите полупериметр: `s = self.get_perimeter() / 2`
     - Шаг 2: S = √(s × (s - a) × (s - b) × (s - c))
     - Используйте `math.sqrt()` для квадратного корня
   
   - **Метод `__str__(self)`:**
     - Формат: `"Triangle ({цвет}) | Sides: {a}, {b}, {c}"`

### Неравенство треугольника - важно!

Треугольник существует только если:
- a + b > c
- a + c > b  
- b + c > a

**Примеры разных треугольников:**

✅ **Разносторонний треугольник (3, 4, 5):**
- 3 + 4 = 7 > 5 ✓
- 3 + 5 = 8 > 4 ✓
- 4 + 5 = 9 > 3 ✓
**Пример 1: Разносторонний треугольник (3-4-5)**
```python
triangle = Triangle("yellow", 3, 4, 5)
print(triangle)
# Вывод: Triangle (yellow) | Sides: 3, 4, 5

print(f"Area: {triangle.get_area():.2f}")
# Вывод: Area: 6.00

print(f"Perimeter: {triangle.get_perimeter():.2f}")
# Вывод: Perimeter: 12.00
```

**Пример 2: Равносторонний треугольник (5-5-5)**
```python
triangle = Triangle("blue", 5, 5, 5)
print(triangle)
# Вывод: Triangle (blue) | Sides: 5, 5, 5

print(f"Area: {triangle.get_area():.2f}")
# Вывод: Area: 10.83

print(f"Perimeter: {triangle.get_perimeter():.2f}")
# Вывод: Perimeter: 15.00
```

**Пример 3: Обработка ошибки - вырожденный треугольник (2-3-5)**
```python
try:
    triangle = Triangle("red", 2, 3, 5)  # 2+3=5, НЕ больше 5!
except ValueError as e:
    print(e)
# Вывод: Invalid triangle! Sum of two sides must be > third.
```

**Пример 4: Обработка ошибки - невозможный треугольник (1-2-10)**
```python
try:
    triangle = Triangle("red", 1, 2, 10)  # 1+2=3 < 10*
- 1 + 2 = 3 < 10 ✗
- **Результат:** НЕВАЛИДНЫЙ (короткие стороны не дотянутся)

### Примеры вывода:

```python
triangle = Triangle("yellow", 3, 4, 5)
print(triangle)
# Вывод: Triangle (yellow) | Sides: 3, 4, 5

print(f"Area: {triangle.get_area():.2f}")
# Вывод: Area: 6.00

print(f"Perimeter: {triangle.get_perimeter():.2f}")
# Вывод: Perimeter: 12.00
```

**Обработка ошибок:**
```python
try:
    triangle = Triangle("red", 1, 2, 10)
except ValueError as e:
    print(e)
# Вывод: Invalid triangle! Sum of two sides must be > third.
```

---

## 🛠️ Этап 6: Вспомогательные функции

**Файл:** `utils/helpers.py`

### Функция 1: `get_valid_float(prompt)`

**Назначение:** Безопасный ввод числа с плавающей точкой.

**Параметры:**
- `prompt` (str) - текст приглашения для пользователя

**Возвращает:**
- `float` - корректно введенное число

**Как реализовать:**
1. Создайте бесконечный цикл `while True:`
2. Используйте блок `try-except`:
   - В `try`: считайте ввод пользователя, преобразуйте в `float` и верните результат
   - В `except ValueError`: выведите сообщение об ошибке и продолжите цикл

**Пример работы:**
```
Enter radius: abc              ← ПОЛЬЗОВАТЕЛЬ вводит (неверный формат)
Error: Invalid input format. Please enter a number.

Enter radius: 5.5              ← ПОЛЬЗОВАТЕЛЬ вводит (правильно)
# Функция возвращает 5.5
```

### Функция 2: `get_valid_index(prompt, max_index)`

**Назначение:** Безопасный ввод индекса элемента из списка.

**Параметры:**
- `prompt` (str) - текст приглашения
- `max_index` (int) - максимально допустимый номер (длина списка)

**Возвращает:**
- `int` - индекс в формате 0-based (начиная с 0)

**Как реализовать:**
1. Бесконечный цикл `while True:`
2. Блок `try-except`:
   - Попытайтесь преобразовать ввод в `int`
   - Проверьте диапазон: `1 <= value <= max_index`
   - Если да - верните `value - 1` (преобразование в 0-based индекс)
   - Если нет - выведите ошибку о неверном диапазоне
   - При `ValueError` - сообщите о неверном формате

**Пример работы:**
```
Enter shape number (1-3): 0                  ← ПОЛЬЗОВАТЕЛЬ вводит (вне диапазона)
Error: Please enter a number between 1 and 3.

Enter shape number (1-3): abc                ← ПОЛЬЗОВАТЕЛЬ вводит (не число)
Error: Invalid input. Please enter an integer number.

Enter shape number (1-3): 2                  ← ПОЛЬЗОВАТЕЛЬ вводит (правильно)
# Функция возвращает 1 (преобразовано из 2 в 0-based)
```

**Важно:** Пользователь вводит номер от 1, функция возвращает индекс от 0!

---

## 🎯 Этап 7: Файлы инициализации пакетов

### Файл: `shapes/__init__.py`

Этот файл делает папку `shapes` пакетом Python и упрощает импорты.

**Что нужно сделать:**
1. Импортируйте все классы фигур из соответствующих модулей
2. олный пример:**
```python
# Shapes package
from .shape import Shape          # ← С ТОЧКОЙ (внутри пакета)
from .circle import Circle        # ← С ТОЧКОЙ
from .rectangle import Rectangle  # ← С ТОЧКОЙ
from .triangle import Triangle    # ← С ТОЧКОЙ

__all__ = ['Shape', 'Circle', 'Rectangle', 'Triangle']
```

**Зачем это нужно?**
- Позволяет писать `from shapes import Circle` вместо `from shapes.circle import Circle`
- `__all__` определяет, что экспортируется при `from shapes import *
__all__ = ['Shape', 'Circle', ...]  # перечислите все классы
```

**Полный пример:**
```python
# Utils package
from .helpers import get_valid_float, get_valid_index  # ← С ТОЧКОЙ!

__all__ = ['get_valid_float', 'get_valid_index']
```

### Файл: `utils/__init__.py`

Аналогично для пакета утилит.

**Что нужно сделать:**
1. Импортируйте функции из `helpers.py`
2. Определите `__all__` с экспортируемыми функциями

---

## 🎮 Этап 8: Главная программа

**Файл:** `main.py`

### Часть 1: Импорты

В начале файла `main.py` импортируйте необходимые классы и функции:

```python
from shapes import Circle, Rectangle, Triangle
from utils import get_valid_float, get_valid_index
```

**⚠️ ВАЖНО:** В `main.py` используем абсолютные импорты (БЕЗ точки `.`), потому что этот файл находится вне пакетов!

**Почему не `from .shapes import Circle`?**
- Точка `.` работает только внутри пакета
- `main.py` находится в корне проекта, не внутри пакета
- Попытка использовать `.shapes` вызовет ошибку: `ImportError: attempted relative import with no known parent package`

### Часть 2: Функция `main()`

**Инициализация:**
- Создайте пустой список `shapes_db = []` для хранения всех фигур

**Главный цикл:**
- Создайте бесконечный цикл `while True:`
- Выведите меню с 7 опциями:
  1. Add new shape
  2. List all shapes
  3. Show shape details
  4. Remove shape
  5. Show sum of all areas
  6. Show sum of all perimeters
  7. Exit
- Запросите выбор пользователя

### Опция 1: Добавление фигуры

**Алгоритм:**
1. Выведите типы фигур: 1. Circle, 2. Rectangle, 3. Triangle
2. Запросите выбор типа
3. Если выбор не в ['1', '2', '3'] - выведите ошибку и `continue`
4. Запросите цвет
5. В зависимости от типа:
   - **Круг:** запросите радиус через `get_valid_float()`, создайте `Circle`
   - **Прямоугольник:** запросите ширину и высоту, создайте `Rectangle`
   - **Треугольник:** запросите три стороны, создайте `Triangle`
6. Добавьте созданный объект в `shapes_db` через `append()`
7. Оберните создание в `try-except ValueError` для перехвата ошибок валидации

**Пример диалога:**
```
Choose an option: 1

Types: 1. Circle, 2. Rectangle, 3. Triangle
Select type: 1
Enter color: blue
Enter radius: 7
Success: Shape added!
```

**С ошибкой:**
```
Choose an option: 1

Types: 1. Circle, 2. Rectangle, 3. Triangle
Select type: 1
Enter color: red
Enter radius: -5
Logic Error: Radius must be positive
```

### Опция 2: Список всех фигур

**Алгоритм:**
1. Проверьте, пустой ли список: `if not shapes_db:`
2. Если пустой - выведите "List is empty."
3. Иначе - используйте `enumerate(shapes_db, 1)` для нумерации с 1
4. Выведите каждую фигуру (автоматически вызовется `__str__()`)

**Пример вывода:**
```
Choose an option: 2            ← Пользователь вводит

# Программа выводит:
--- ALL SHAPES ---
1. Circle (blue) | R: 7.0
2. Rectangle (green) | 10.0 x 5.0
3. Triangle (yellow) | Sides: 3, 4, 5
```

### Опция 3: Детали фигуры

**Алгоритм:**
1. Проверьте на пустоту списка
2. Запросите номер фигуры через `get_valid_index()`
3. Получите объект фигуры: `shape = shapes_db[idx]`
4. Выведите:
   - Информацию о фигуре
   - Площадь с форматированием `.2f` (2 знака после запятой)
   - Периметр с форматированием `.2f`

**Пример вывода:**
```
Choose an option: 3            ← Пользователь вводит

# Программа выводит:
--- SHAPE DETAILS ---
Enter shape number (1-3): 1    ← Пользователь вводит

# Программа выводит:
------------------------------
Info:      Circle (blue) | R: 7.0
Area:      153.94
Perimeter: 43.98
------------------------------
```

### Опция 4: Удаление фигуры

**Алгоритм:**
1. Проверьте на пустоту
2. Запросите номер фигуры через `get_valid_index()`
3. Удалите фигуру: `removed = shapes_db.pop(idx)`
4. Выведите подтверждение с информацией об удаленной фигуре

**Пример вывода:**
```
Choose an option: 4                          ← Пользователь вводит

# Программа выводит:
--- REMOVE SHAPE ---
Enter shape number to remove (1-3): 2        ← Пользователь вводит

# Программа выводит:
Success: Removed Rectangle (green) | 10.0 x 5.0
```

### Опция 5: Сумма площадей

**Алгоритм:**
1. Используйте функцию `sum()` с генератором
2. Генератор: `s.get_area() for s in shapes_db`
3. Выведите результат с количеством фигур и суммой

**Пример вывода:**
```
Choose an option: 5            ← Пользователь вводит

# Программа выводит:
Total Area of 3 shapes: 209.94
```

**Как работает генератор:**
```python
# Для каждой фигуры s в списке shapes_db
# вызываем метод get_area() и суммируем результаты
total = sum(s.get_area() for s in shapes_db)
```

### Опция 6: Сумма периметров

**Алгоритм:**
- Аналогично опции 5, но вызывайте `get_perimeter()`

**Пример вывода:**
```
Choose an option: 6                    ← Пользователь вводит

# Программа выводит:
Total Perimeter of 3 shapes: 76.56
```

### Опция 7: Выход

**Алгоритм:**
1. Выведите "Goodbye!"
2. Используйте `break` для выхода из цикла

### Обработка неверного ввода

**Алгоритм:**
- Добавьте блок `else:` который выводит "Invalid option. Try again."

### Часть 3: Точка входа

В конце файла добавьте:
```python
if __name__ == "__main__":
    main()
```

**Что это значит:**
- `__name__ == "__main__"` истинно только при прямом запуске файла
- Это предотвращает запуск `main()` при импорте модуля

---

## 🚀 Этап 9: Запуск программы

### Из терминала:

```bash
cd final_task
python main.py
```

Или:
```bash
python3 main.py
```

### Ожидаемый вывод:

```
--- SHAPES MANAGER V3.0 ---
1. Add new shape
2. List all shapes
3. Show shape details
4. Remove shape
5. Show sum of all areas
6. Show sum of all perimeters
7. Exit
Choose an option:
```

---

## 📝 Полный пример сессии работы

### Сценарий: Добавление фигур и работа с ними

> **Обозначения:**
> - Строки без стрелки `←` = вывод программы
> - Строки со стрелкой `←` = ввод пользователя

```
# ========== ПРОГРАММА ВЫВОДИТ МЕНЮ ==========
--- SHAPES MANAGER V3.0 ---
1. Add new shape
2. List all shapes
3. Show shape details
4. Remove shape
5. Show sum of all areas
6. Show sum of all perimeters
7. Exit
Choose an option: 1                    ← ПОЛЬЗОВАТЕЛЬ ВЫБИРАЕТ ОПЦИЮ 1

# ========== ДОБАВЛЯЕМ КРУГ ==========
Types: 1. Circle, 2. Rectangle, 3. Triangle
Select type: 1                         ← ПОЛЬЗОВАТЕЛЬ: выбирает круг
Enter color: blue                      ← ПОЛЬЗОВАТЕЛЬ: вводит цвет
Enter radius: 7                        ← ПОЛЬЗОВАТЕЛЬ: вводит радиус
Success: Shape added!

# ========== МЕНЮ СНОВА ==========
--- SHAPES MANAGER V3.0 ---
1. Add new shape
2. List all shapes
3. Show shape details
4. Remove shape
5. Show sum of all areas
6. Show sum of all perimeters
7. Exit
Choose an option: 1                    ← ПОЛЬЗОВАТЕЛЬ ВЫБИРАЕТ ОПЦИЮ 1

# ========== ДОБАВЛЯЕМ ПРЯМОУГОЛЬНИК ==========
Types: 1. Circle, 2. Rectangle, 3. Triangle
Select type: 2                         ← ПОЛЬЗОВАТЕЛЬ: выбирает прямоугольник
Enter color: green                     ← ПОЛЬЗОВАТЕЛЬ: вводит цвет
Enter width: 10                        ← ПОЛЬЗОВАТЕЛЬ: вводит ширину
Enter height: 5                        ← ПОЛЬЗОВАТЕЛЬ: вводит высоту
Success: Shape added!

# ========== МЕНЮ СНОВА ==========
--- SHAPES MANAGER V3.0 ---
1. Add new shape
2. List all shapes
3. Show shape details
4. Remove shape
5. Show sum of all areas
6. Show sum of all perimeters
7. Exit
Choose an option: 1                    ← ПОЛЬЗОВАТЕЛЬ ВЫБИРАЕТ ОПЦИЮ 1

# ========== ДОБАВЛЯЕМ ТРЕУГОЛЬНИК ==========
Types: 1. Circle, 2. Rectangle, 3. Triangle
Select type: 3                         ← ПОЛЬЗОВАТЕЛЬ: выбирает треугольник
Enter color: yellow                    ← ПОЛЬЗОВАТЕЛЬ: вводит цвет
Side A: 3                              ← ПОЛЬЗОВАТЕЛЬ: вводит сторону A
Side B: 4                              ← ПОЛЬЗОВАТЕЛЬ: вводит сторону B
Side C: 5                              ← ПОЛЬЗОВАТЕЛЬ: вводит сторону C
Success: Shape added!

# ========== МЕНЮ СНОВА ==========
--- SHAPES MANAGER V3.0 ---
1. Add new shape
2. List all shapes
3. Show shape details
4. Remove shape
5. Show sum of all areas
6. Show sum of all perimeters
7. Exit
Choose an option: 2                    ← ПОЛЬЗОВАТЕЛЬ ВЫБИРАЕТ ОПЦИЮ 2 (Список)

# ========== ПРОГРАММА ВЫВОДИТ ВСЕ ФИГУРЫ ==========
--- ALL SHAPES ---
1. Circle (blue) | R: 7.0
2. Rectangle (green) | 10.0 x 5.0
3. Triangle (yellow) | Sides: 3, 4, 5

# ========== МЕНЮ СНОВА ==========
--- SHAPES MANAGER V3.0 ---
1. Add new shape
2. List all shapes
3. Show shape details
4. Remove shape
5. Show sum of all areas
6. Show sum of all perimeters
7. Exit
Choose an option: 3                    ← ПОЛЬЗОВАТЕЛЬ ВЫБИРАЕТ ОПЦИЮ 3 (Детали)

# ========== ПОКАЗЫВАЕМ ДЕТАЛИ ФИГУРЫ ==========
--- SHAPE DETAILS ---
Enter shape number (1-3): 1            ← ПОЛЬЗОВАТЕЛЬ: выбирает фигуру №1
------------------------------
Info:      Circle (blue) | R: 7.0
Area:      153.94
Perimeter: 43.98
------------------------------

# ========== МЕНЮ СНОВА ==========
--- SHAPES MANAGER V3.0 ---
1. Add new shape
2. List all shapes
3. Show shape details
4. Remove shape
5. Show sum of all areas
6. Show sum of all perimeters
7. Exit
Choose an option: 5                    ← ПОЛЬЗОВАТЕЛЬ ВЫБИРАЕТ ОПЦИЮ 5 (Сумма площадей)
Total Area of 3 shapes: 209.94

# ========== МЕНЮ СНОВА ==========
--- SHAPES MANAGER V3.0 ---
1. Add new shape
2. List all shapes
3. Show shape details
4. Remove shape
5. Show sum of all areas
6. Show sum of all perimeters
7. Exit
Choose an option: 6                    ← ПОЛЬЗОВАТЕЛЬ ВЫБИРАЕТ ОПЦИЮ 6 (Сумма периметров)
Total Perimeter of 3 shapes: 105.98

# ========== МЕНЮ СНОВА ==========
--- SHAPES MANAGER V3.0 ---
1. Add new shape
2. List all shapes
3. Show shape details
4. Remove shape
5. Show sum of all areas
6. Show sum of all perimeters
7. Exit
Choose an option: 4                    ← ПОЛЬЗОВАТЕЛЬ ВЫБИРАЕТ ОПЦИЮ 4 (Удалить)

# ========== УДАЛЯЕМ ФИГУРУ ==========
--- REMOVE SHAPE ---
Enter shape number to remove (1-3): 2  ← ПОЛЬЗОВАТЕЛЬ: выбирает удалить №2
Success: Removed Rectangle (green) | 10.0 x 5.0

# ========== МЕНЮ СНОВА ==========
--- SHAPES MANAGER V3.0 ---
1. Add new shape
2. List all shapes
3. Show shape details
4. Remove shape
5. Show sum of all areas
6. Show sum of all perimeters
7. Exit
Choose an option: 2                    ← ПОЛЬЗОВАТЕЛЬ ВЫБИРАЕТ ОПЦИЮ 2 (Список)

# ========== ТЕПЕРЬ ТОЛЬКО 2 ФИГУРЫ ==========
--- ALL SHAPES ---
1. Circle (blue) | R: 7.0
2. Triangle (yellow) | Sides: 3, 4, 5

# ========== МЕНЮ СНОВА ==========
--- SHAPES MANAGER V3.0 ---
1. Add new shape
2. List all shapes
3. Show shape details
4. Remove shape
5. Show sum of all areas
6. Show sum of all perimeters
7. Exit
Choose an option: 7                    ← ПОЛЬЗОВАТЕЛЬ ВЫБИРАЕТ ОПЦИЮ 7 (Выход)
Goodbye!
```

---

## 🎓 Ключевые концепции для изучения

### 1. ООП (Объектно-ориентированное программирование)

- **Наследование:** `Circle`, `Rectangle`, `Triangle` наследуются от `Shape`
  ```python
  class Circle(Shape):  # Circle наследует Shape
      def __init__(self, color, radius):
          super().__init__(color)  # Вызов конструктора родителя
  ```

- **Инкапсуляция:** Защищенные атрибуты с префиксом `_`
  ```python
  self._color = color   # Защищенный атрибут
  self._radius = radius # Защищенный атрибут
  ```

- **Полиморфизм:** Все классы имеют методы `get_area()` и `get_perimeter()`, но реализация разная
  ```python
  # У каждого класса своя реализация
  circle.get_area()     # Использует формулу круга
  rectangle.get_area()  # Использует формулу прямоугольника
  triangle.get_area()   # Использует формулу Герона
  ```

### 2. Обработка исключений

**Зачем нужны `try-except` блоки?**

Они **защищают программу от краха**. Когда возникает ошибка:
- **БЕЗ try-except**: программа немедленно завершается с Traceback
- **С try-except**: ошибка перехватывается, выводится сообщение, программа продолжает работу

**Пример:**
```python
try:
    # Опасный код, который может выбросить исключение
    circle = Circle("red", -5)  # ValueError!
except ValueError as e:
    # Ошибка поймана, программа продолжает работать
    print(f"Ошибка: {e}")
# Программа продолжается здесь ↓
print("Меню появится снова")
```

**Типы исключений:**
- `ValueError` - используется для валидации данных (неверные значения)
- Создание собственных исключений в конструкторах с помощью `raise ValueError("сообщение")`

### 3. Работа с пакетами и модулями

- **Пакет** - папка с файлом `__init__.py`
- **Модуль** - файл `.py`

**ВАЖНО: Типы импортов**

| Где находится файл | Тип импорта | Синтаксис | Когда использовать |
|-------------------|-------------|-----------|-------------------|
| **Внутри пакета** | Относительный | `from .module import Class` | Файл находится в папке `shapes/` или `utils/` |
| **Вне пакета** | Абсолютный | `from package import Class` | Файл `main.py` в корне проекта |

**Примеры:**

**В `shapes/circle.py` (ВНУТРИ пакета):**
```python
from .shape import Shape  # ← С ТОЧКОЙ! Относительный импорт
```

**В `shapes/__init__.py` (ВНУТРИ пакета):**
```python
from .shape import Shape
from .circle import Circle  # ← С ТОЧКОЙ! Относительный импорт
```

**В `main.py` (ВНЕ пакета, корень проекта):**
```python
from shapes import Circle  # ← БЕЗ ТОЧКИ! Абсолютный импорт
from utils import get_valid_float  # ← БЕЗ ТОЧКИ!
```

**Почему так?**
- Точка `.` означает "текущий пакет" - работает только внутри пакета
- В `main.py` нет "текущего пакета", поэтому используем абсолютный импорт

### 4. Работа со списками

- `append()` - добавление элемента
- `pop(index)` - удаление элемента по индексу
- `enumerate(list, start)` - получение индекса и элемента
- Генераторы: `(s.get_area() for s in shapes_db)`

### 5. Встроенные функции

- `sum()` - суммирование
- `len()` - длина списка
- `input()` - ввод данных
- `print()` - вывод данных
- `float()`, `int()` - преобразование типов

### 6. Форматирование строк

- f-строки: `f"Circle ({self._color})"`
- Форматирование чисел: `{value:.2f}` (2 знака после запятой)

### 7. Математические вычисления

- Формула площади круга: π × r²
- Формула Герона для площади треугольника
- Модуль `math` для математических операций

---

## ✅ Критерии оценки

### Обязательные требования (минимум на "удовлетворительно"):

1. ✅ Все 4 класса реализованы (`Shape`, `Circle`, `Rectangle`, `Triangle`)
2. ✅ Реализованы методы `get_area()` и `get_perimeter()` для всех фигур
3. ✅ Валидация данных в конструкторах (проверка на положительные числа)
4. ✅ Правильная структура пакетов (папки `shapes/` и `utils/`)
5. ✅ Файлы `__init__.py` настроены корректно
6. ✅ Реализовано меню с 7 опциями в `main.py`
7. ✅ Программа работает без ошибок

### Дополнительные баллы (на "хорошо" и "отлично"):

1. ⭐ Корректная обработка исключений во всех местах
2. ⭐ Проверка неравенства треугольника
3. ⭐ Вспомогательные функции `get_valid_float()` и `get_valid_index()`
4. ⭐ Красивый вывод с форматированием
5. ⭐ Docstrings для функций и классов
6. ⭐ Комментарии в коде
7. ⭐ Правильное использование относительных и абсолютных импортов

---

## 🐛 Типичные ошибки и как их избежать

### ❓ Важный вопрос: "Зачем try-except? Это ведь остановит код?"

**Ответ:** Всё наоборот! `try-except` **ПРЕДОТВРАЩАЕТ** остановку программы, а не вызывает её!

**Аналогия с подушкой безопасности в машине:**
- **Без подушки (без try-except):** При аварии → травма → конец поездки 💥
- **С подушкой (с try-except):** При аварии → подушка смягчает удар → едем дальше ✅

**Демонстрация:**

❌ **Вариант 1: БЕЗ try-except (программа УПАДЕТ):**
```python
while True:
    print("--- MENU ---")
    choice = input("Выбор: ")
    
    if choice == '1':
        r = float(input("Радиус: "))
        circle = Circle("red", r)  # Если r = -5 → программа УПАДЕТ!
        print("Добавлено!")
```

**Что видит пользователь:**
```
--- MENU ---
Выбор: 1                                 ← ПОЛЬЗОВАТЕЛЬ вводит
Радиус: -5                               ← ПОЛЬЗОВАТЕЛЬ вводит (неверное значение!)

# ПРОГРАММА выводит:
Traceback (most recent call last):
  File "main.py", line 45, in <module>
    circle = Circle("red", r)
ValueError: Radius must be positive

$ ← ПРОГРАММА ЗАВЕРШЕНА! Пользователь вернулся в терминал. 💥
```

✅ **Вариант 2: С try-except (программа ПРОДОЛЖИТ работу):**
```python
while True:
    print("--- MENU ---")
    choice = input("Выбор: ")
    
    if choice == '1':
        try:
            r = float(input("Радиус: "))
            circle = Circle("red", r)  # Если r = -5 → исключение ПОЙМАНО!
            print("Добавлено!")
        except ValueError as e:
            print(f"Ошибка: {e}")  # Показали сообщение пользователю
    # Цикл while продолжается! ↓
```

**Что видит пользователь:**
```
--- MENU ---
Выбор: 1                                 ← ПОЛЬЗОВАТЕЛЬ вводит
Радиус: -5                               ← ПОЛЬЗОВАТЕЛЬ вводит (неверное значение!)

# ПРОГРАММА выводит:
Ошибка: Radius must be positive

--- MENU ---                             ← ПРОГРАММА РАБОТАЕТ! Меню появилось снова! ✅
Выбор: 1                                 ← Пользователь может попробовать еще раз
Радиус: 10                               ← ПОЛЬЗОВАТЕЛЬ вводит (правильное значение)
Добавлено!
```

**Вывод:** `try-except` - это **защита от краша**, а не причина краша!

---

### Ошибка 1: Забыли вызвать `super().__init__()`

❌ **Неправильно:**
```python
class Circle(Shape):
    def __init__(self, color, radius):
        self._radius = radius  # Забыли вызвать super()!
```

**Проблема:** Атрибут `_color` не инициализируется, родительский конструктор не выполняется.

✅ **Правильно:**
```python
class Circle(Shape):
    def __init__(self, color, radius):
        super().__init__(color)  # Инициализируем родительский класс
        self._radius = radius
```

---

### Ошибка 2: Не преобразовали индекс в `get_valid_index()`

❌ **Неправильно:**
```python
def get_valid_index(prompt, max_index):
    value = int(input(prompt))
    if 1 <= value <= max_index:
        return value  # Вернули 1-based индекс! (прямая линия, а не треугольник с площадью)
```

**Проблема:** Пользователь вводит 1, а вы пытаетесь обратиться к `shapes_db[1]` (второй элемент).

✅ **Правильно:**
```python
def get_valid_index(prompt, max_index):
    value = int(input(prompt))
    if 1 <= value <= max_index:
        return value - 1  # Преобразовали в 0-based
```

---

### Ошибка 3: Неправильная проверка треугольника

❌ **Неправильно:**
```python
if (a + b < c) or (a + c < b) or (b + c < a):  # Используется <, а не <=
    raise ValueError("Invalid triangle!")
```

**Проблема:** Треугольник с сторонами 2, 3, 5 (где 2+3=5) пройдет валидацию, но это вырожденный треугольник.

✅ **Правильно:**
```python
if (a + b <= c) or (a + c <= b) or (b + c <= a):  # Должно быть <=
    raise ValueError("Invalid triangle!")
```

---

### Ошибка 4: Забыли импортировать `math`

❌ **Неправильно:**
```python
def get_area(self):
    return 3.14 * (self._radius ** 2)  # Приближенное значение
```

**Проблема:** Неточные вычисления.

✅ **Правильно:**
```python
import math

def get_area(self):
    return math.pi * (self._radius ** 2)  # Точное значение
```

---

### Ошибка 5: Не обработали пустой список

❌ **Неправильно:**
```python
elif choice == '3':
    idx = get_valid_index(...)  # Если список пуст - будет ошибка!
    shape = shapes_db[idx]
```

**Проблема:** При пустом списке `get_valid_index()` попросит ввести число от 1 до 0, что невозможно.

✅ **Правильно:**
```python          # ← БЕЗ точки - не найдет модуль!
import circle
```

**Проблема:** Внутри пакета нужны относительные импорты (С ТОЧКОЙ).

✅ **Правильно:**
```python
# shapes/__init__.py
from .shape import Shape          # ← С ТОЧКОЙ!
from .circle import Circle        # ← С ТОЧКОЙ!
from .rectangle import Rectangle  # ← С ТОЧКОЙ!
from .triangle import Triangle    # ← С ТОЧКОЙ!

__all__ = ['Shape', 'Circle', 'Rectangle', 'Triangle']
```

---

### Ошибка 6.1: Использование точки в main.py

❌ **Неправильно:**
```python
# main.py (в корне проекта)
from .shapes import Circle  # ← ОШИБКА! Точка только внутри пакета!
```

**Проблема:** `main.py` не находится внутри пакета, поэтому относительный импорт невозможен.

**Получите:**
```
ImportError: attempted relative import with no known parent package
```

✅ **Правильно:**
```python
# main.py (в корне проекта)
from shapes import Circle  # ← БЕЗ ТОЧКИ! Абсолютный импорт!
from utils import get_valid_float
# shapes/__init__.py
import shape
import circle
```

**Проблема:** Нужны относительные импорты.

✅ **Правильно:**
```python
# shapes/__init__.py
from .shape import Shape
from .circle import Circle
from .rectangle import Rectangle
from .triangle import Triangle

__all__ = ['Shape', 'Circle', 'Rectangle', 'Triangle']
```

---

### Ошибка 7: Циклический импорт

❌ **Неправильно:**
```python
# shapes/shape.py
from shapes.circle import Circle  # Не делайте так!

class Shape:
    pass
```

**Проблема:** Создается циклическая зависимость.

✅ **Правильно:**
Базовый класс не должен импортировать дочерние классы.

---

## 🔍 Контрольные вопросы для самопроверки

1. Что такое защищенный атрибут и зачем используется префикс `_`?
2. Почему в классе `Shape` методы `get_area()` и `get_perimeter()` возвращают 0.0?
3. Что произойдет, если создать круг с радиусом -5?
4. **Зачем нужен try-except? Он останавливает программу или предотвращает остановку?**
5. Зачем в функции `get_valid_index()` вычитается 1 перед возвратом?
6. Как работает формула Герона для вычисления площади треугольника?
7. Что делает функция `enumerate(shapes_db, 1)`?
8. Зачем нужен блок `if __name__ == "__main__":`?
9. Почему используется генератор в `sum(s.get_area() for s in shapes_db)`?
10. Что произойдет, если пользователь введет букву вместо числа при выборе опции?
11. Как работает неравенство треугольника?
12. В чем разница между относительным (`from .shape import Shape`) и абсолютным (`from shapes import Shape`) импортом?
13. Зачем нужен файл `__init__.py`?
14. Что делает `super().__init__(color)`?
15. **Когда выбрасывать исключение (raise), а когда ловить (try-except)?**
16. Как работает метод `pop()` для списков?

---

## 📚 Дополнительные задания (по желанию)

### Уровень 1: Добавить новые функции

1. **Поиск фигуры по цвету**
   - Добавьте опцию "8. Find shapes by color"
   - Выведите все фигуры заданного цвета

2. **Статистика**
   - Добавьте опцию "9. Show statistics"
   - Выведите: количество фигур каждого типа, средняя площадь, средний периметр

### Уровень 2: Новые фигуры

3. **Добавьте класс Square (Квадрат)**
   - Наследуется от `Rectangle`
   - Конструктор принимает только `color` и `side`
   - Автоматически устанавливает `width = height = side`

4. **Добавьте класс Ellipse (Эллипс)**
   - Наследуется от `Shape`
   - Конструктор: `color`, `a` (большая полуось), `b` (малая полуось)
   - Площадь: S = π × a × b
   - Периметр: приближенная формула Рамануджана

### Уровень 3: Сохранение данных

5. **Сохранение в файл**
   - Добавьте опцию "10. Save to file"
   - Сохраните все фигуры в JSON или CSV файл

6. **Загрузка из файла**
   - Добавьте опцию "11. Load from file"
   - Загрузите фигуры из файла

---

## 📚 Полезные ресурсы

### Документация Python:
- [Классы в Python](https://docs.python.org/3/tutorial/classes.html)
- [Обработка исключений](https://docs.python.org/3/tutorial/errors.html)
- [Модули и пакеты](https://docs.python.org/3/tutorial/modules.html)
- [Модуль math](https://docs.python.org/3/library/math.html)

### Математические формулы:
- [Формула площади круга](https://ru.wikipedia.org/wiki/Круг)
- [Формула Герона](https://ru.wikipedia.org/wiki/Формула_Герона)
- [Неравенство треугольника](https://ru.wikipedia.org/wiki/Неравенство_треугольника)

---

## 🎯 Заключение

Это задание демонстрирует:
- **ООП на практике**: реальное применение наследования, инкапсуляции и полиморфизма
- **Модульность**: разделение кода на логические пакеты и модули
- **Обработку ошибок**: корректная валидация пользовательского ввода
- **Работу со списками**: добавление, удаление, итерация
- **Математические вычисления**: применение формул из геометрии
- **Интерактивный интерфейс**: создание консольного приложения с меню

### Порядок выполнения:

1. ✅ Создайте структуру папок и файлов
2. ✅ Реализуйте базовый класс `Shape`
3. ✅ Реализуйте классы-наследники: `Circle`, `Rectangle`, `Triangle`
4. ✅ Создайте вспомогательные функции
5. ✅ Настройте файлы `__init__.py`
6. ✅ Реализуйте главную программу `main.py`
7. ✅ Протестируйте все функции
8. ✅ Проверьте обработку ошибок

Успехов в выполнении задания! 🚀

---

**Версия:** 4.0  
**Дата:** 2026  
**Автор:** Python Course Final Task  
**Тип проекта:** Модульное консольное приложение с ООП
